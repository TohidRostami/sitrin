# یادداشت‌های یکپارچه‌سازی فرانت‌اند سیترین

این فایل مستقیماً برای تیم فنی‌ست: هرچیزی که در ساخت این فرانت‌اند «تصمیم گرفته
شده» یا «ساده‌سازی شده» نسبت به طراحی اصلی یا نسبت به یک فروشگاه کامل، اینجا
مستند شده — تا کسی گیج نشود که چرا فلان بخش این‌شکلی‌ست.

## ۱. قبل از هر چیز: این پروژه را چطور اجرا کنم؟

```bash
npm install
cp .env.example .env          # BETTER_AUTH_SECRET را با: openssl rand -base64 32 پر کنید
npm run db:push
npm run db:seed               # ۱۴ محصول نمونه در ۵ دسته‌بندی می‌سازد (بدون عکس — بخش ۳ را ببینید)
npm run dev
```

> ⚠️ در سندباکسی که این پروژه در آن ساخته شد، دسترسی شبکه به
> `binaries.prisma.sh` مسدود بود، پس `prisma generate` / `db push` /
> `db seed` واقعاً اجرا **نشدند** — فقط با یک Prisma Client جعلیِ موقت (که قبل
> از تحویل حذف شد) تایپ‌اسکریپت، ESLint، و `next build` کامل تست شدند و بدون
> خطا رد شدند. روی سیستم شما که به اینترنت عادی دسترسی دارد، این دستورها باید
> بدون مشکل کار کنند؛ اگر مشکلی پیش آمد اول از همه‌چیز مطمئن شوید `npm install`
> کامل تمام شده (چون `postinstall` خودش `prisma generate` را صدا می‌زند).

## ۲. برند: «سیترین» در برابر «سیترین»

فایل `README.md` که برایم فرستادید، پروژه (و دیتابیس) را «سیترین — فروشگاه
پوشاک مردانه» معرفی می‌کند، اما فایل طراحی که باید پیاده می‌کردم برای برند
«سیترین» (فروشگاه اسنیکر) بود. تمام متن رو-به-کاربر (نام سایت، لوگو، متادیتا،
محتوای `content/site-config.json`) را طبق طراحی روی **سیترین** گذاشتم.
`package.json.name` و placeholder ایمیل تلفنی (`...@sitrin-phone.local` در
`lib/auth.ts`) را دست‌نخورده نگه داشتم چون تغییرشان صرفاً کازمتیک بود و ریسک
بی‌مورد داشت — اگر می‌خواهید کاملاً یکدست شود، این دو مورد را دستی عوض کنید.

## ۳. عکس محصولات — عمداً خالی

سه عکسی که همراه فایل طراحی بود، موکاپ‌های خود ابزار طراحی از برندهای دیگر
بودند (نایک، آدیداس، و یک برند فرضی)، نه عکس محصولات واقعی — نه در طراحی نهایی
قابل استفاده بودند و نه در سایت واقعی. طبق تاییدتان، تمام جاهایی که باید عکس
باشد (کارت محصول، گالری PDP، هیرو، دسته‌بندی‌ها) یک placeholder گرافیکی ساده
(خط‌نگاره‌ی انتزاعی کفش، در `components/shared/product-image.tsx`) نشان
می‌دهند وقتی `url` عکس خالی باشد. `prisma/seed.ts` هم عمداً هیچ `ProductImage`
نمی‌سازد. عکس واقعی را از پنل ادمین (بعد از وصل‌شدن Object Storage آروان‌کلاد،
طبق بخش ۷ فایل اصلی `README.md`) آپلود کنید.

## ۴. ساده‌سازی‌های واقعی نسبت به طراحی (چون `schema.prisma` این‌ها را پشتیبانی نمی‌کرد)

هرکدام از این‌ها را می‌شود بعداً با اضافه‌کردن مدل/فیلد به schema گسترش داد؛
پایین هر کدام نوشته‌ام دقیقاً چه چیزی لازم است.

### چک‌اوت مهمان امکان‌پذیر نیست

`Order.userId` و `Address.userId` هر دو الزامی (`String`, نه nullable)
هستند. یعنی سبد خرید مهمان (localStorage) کار می‌کند، اما صفحه‌ی
`/checkout` همیشه لاگین می‌خواهد (`redirect("/login?next=/checkout")` در
`app/(shop)/checkout/page.tsx`). برای چک‌اوت مهمان واقعی، `Order.userId` و
`Address.userId` باید nullable شوند و یک فیلد ایمیل/تلفن مهمان به `Order`
اضافه شود.

### روش ارسال: یک گزینه، نه چند شرکت حمل‌ونقل

طراحی دو کارت انتخابی نشان می‌داد («ارسال اکسپرس» رایگان / «پست پیشتاز»
۸۵٬۰۰۰)، اما `SiteSetting` فقط `standardShippingCost` (یک عدد ثابت) و
`freeShippingThreshold` دارد — یعنی از نظر مدل داده، فقط «یک» هزینه ارسال
وجود دارد که بالای یک سقف مبلغ رایگان می‌شود. در `checkout-form.tsx` همان
یک گزینه‌ی محاسبه‌شده را با همان استایل کارت طراحی نشان می‌دهم (رادیو همیشه
انتخاب‌شده). برای چند روش ارسال واقعی، یک مدل `ShippingMethod` (نام، هزینه،
زمان تحویل) لازم است که `Order` به آن رفرنس بدهد.

### روش پرداخت: فقط درگاه آنلاین

طراحی سه گزینه داشت (درگاه بانکی / کیف پول سیترین / پرداخت درب منزل). فقط
«درگاه بانکی» را پیاده کردم چون تنها چیزی‌ست که `lib/payment/` برایش seam
دارد. «کیف پول» و «پرداخت درب منزل» را کلاً حذف کردم به‌جای اینکه ظاهری
غیرفعال نگه‌شان دارم — چون یک گزینه‌ی غیرفعال روی صفحه‌ی پرداخت واقعی
گیج‌کننده است. برای کیف پول، یک مدل `WalletTransaction` (یا فیلد `walletBalance`
روی `User`) لازم است؛ صفحه‌ی `/account/wallet` را همین الان به‌عنوان
placeholder صادق («به‌زودی») ساخته‌ام تا آیتم منو بی‌معنی نباشد.

### علاقه‌مندی‌ها فقط روی همان مرورگر

هیچ مدل Wishlist/Favorite در schema نیست، پس `lib/favorites-store.ts` کاملاً
سمت کلاینت (zustand + localStorage) است — بین دستگاه‌ها یا بعد از پاک‌کردن
مرورگر سینک نمی‌شود. برای نسخه‌ی واقعی: یک مدل `FavoriteProduct` (رابطه‌ی
many-to-many بین `User` و `Product`) اضافه کنید و این store را با
server action جایگزین کنید — ساختار کامپوننت (`app/(shop)/account/favorites/page.tsx`)
طوری نوشته شده که این جایگزینی ساده باشد.

### پیام تماس و عضویت خبرنامه فقط لاگ می‌شوند

`schema.prisma` مدلی برای پیام‌های فرم تماس یا مشترکین خبرنامه ندارد و هیچ
سرویس ایمیل/ESP در `package.json` نصب نیست. `lib/mail.ts` و
`app/api/newsletter/route.ts` فقط در کنسول سرور لاگ می‌کنند و کامنت دارند
دقیقاً کجا باید یک سرویس واقعی (Resend/SendGrid برای ایمیل، Mailchimp و
مشابه برای خبرنامه) وصل شود.

### ورود با گوگل: دکمه هست، پیاده‌سازی نیست

`lib/auth.ts` که فرستادید هیچ `socialProviders` ندارد. دکمه‌ی «ورود با گوگل»
را طبق طراحی نگه داشتم اما `disabled` است با یک tooltip. برای فعال‌کردن
واقعی‌اش: `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` به `.env` و
`socialProviders: { google: {...} }` به `lib/auth.ts` اضافه کنید، همان‌جا در
`components/auth/auth-panel.tsx` دکمه را از حالت disabled خارج و
`authClient.signIn.social({ provider: "google" })` را صدا بزنید.

### ورود با پیامک: کاملاً واقعی و کار می‌کند

برخلاف موارد بالا، این یکی کامل پیاده شده — `components/auth/phone-otp-dialog.tsx`
با `authClient.phoneNumber.sendOtp` / `.verify` (پلاگین phoneNumber که در
`lib/auth.ts` فعال است) کار می‌کند. فقط یادتان باشد `lib/sms.ts` را طبق
راهنمای داخلش به Kavenegar وصل کنید — تا آن موقع، کد OTP در کنسول سرور لاگ
می‌شود (دقیقاً همان چیزی که `README.md` اصلی هم توضیح داده بود).

## ۵. چیزهایی که عمداً نساختم (خارج از دامنه‌ی «فرانت‌اند فروشگاه»)

- **پنل ادمین (`/admin`)** — طبق گفته‌ی خودتان از قبل ساخته شده.
- **`scripts/make-admin.ts`** — در `package.json` رفرنس شده اما فایلش را
  نفرستادید؛ چون گفتید بک‌اند/ادمین آماده است، فرض کردم این اسکریپت هم از قبل
  در پروژه‌ی واقعی‌تان هست. اگر نیست بگویید تا بنویسمش.
- **`lib/storage.ts`** (آپلود عکس به آروان‌کلاد) — این هم صرفاً برای فرم
  محصول در پنل ادمین کاربرد دارد؛ فرانت‌اند فروشگاه فقط عکس را _نمایش_
  می‌دهد (از `url` ذخیره‌شده در دیتابیس)، آپلود نمی‌کند.

## ۶. تنظیمات ظاهری قابل‌ویرایش بدون تغییر کد

تمام متن‌های ثابت (نام سایت، منو، متن هیرو، درباره ما، اطلاعات تماس، ستون‌های
فوتر) در `content/site-config.json` هستند — دقیقاً همان الگویی که
`README.md` اصلی توصیف کرده بود (`content/site-config.json` برای متن،
`app/globals.css` برای رنگ‌های تم). اگر رنگ برند را در `app/globals.css`
(بلاک `@theme`) عوض کردید، مقادیر مستندسازی‌شده در بخش `theme` همان JSON را
هم برای هماهنگی به‌روز کنید.
